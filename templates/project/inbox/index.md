# Project inbox index

Allowed states are `pending`, `confirmed`, `routed`, and `archived`, as defined
by the workspace inbox rules.

When asked about provisional or unrouted content, inspect `pending` and
`confirmed` entries and read their source files. A missing index means the
inventory is unavailable; it never proves that the inbox is empty.

This file is the canonical inventory of captures routed to this project's
inbox. Juno must update it whenever a capture is appended or its review state
changes.

| Captured | Capture ID | Status | Summary | Source file |
|---|---|---|---|---|
